Add WayForPay payment integration to the frontend. 
Backend is already built at https://gradus-ai.onrender.com

STEP 1 — Add to index.html <head>:
<script src="https://secure.wayforpay.com/server/pay-widget.js"></script>

STEP 2 — Create a Pricing page with 3 tiers:
- Free: 5 questions/day, no payment
- Standard: $7/month (show "≈ X ₴ за курсом НБУ" fetched from GET https://gradus-ai.onrender.com/api/payments/uah-rate)
- Premium: $10/month (same UAH display)

STEP 3 — Payment button handler for Standard and Premium:

const handlePayment = async (tier) => {
    setPaymentLoading(true);
    try {
        const response = await fetch(
            'https://gradus-ai.onrender.com/api/payments/create-checkout',
            {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ email: userEmail, tier: tier })
            }
        );
        const params = await response.json();
        const wayforpay = new window.Wayforpay();
        wayforpay.run({
            merchantAccount: params.merchantAccount,
            merchantDomainName: params.merchantDomainName,
            authorizationType: "SimpleSignature",
            merchantSignature: params.merchantSignature,
            orderReference: params.orderReference,
            orderDate: params.orderDate,
            amount: params.amount,
            currency: "UAH",
            productName: params.productName,
            productPrice: params.productPrice,
            productCount: params.productCount,
            language: "UA",
            straightWidget: true
        },
        function(response) {
            setPaymentLoading(false);
            setUserTier(tier);
            showNotification('Підписку активовано! Дякуємо 🎉');
        },
        function(response) {
            setPaymentLoading(false);
            showNotification('Оплата відхилена. Спробуйте ще раз.', 'error');
        },
        function(response) {
            setPaymentLoading(false);
            showNotification('Платіж обробляється...', 'info');
        });
    } catch (error) {
        setPaymentLoading(false);
        showNotification('Помилка оплати. Спробуйте пізніше.', 'error');
    }
};

STEP 4 — Add /pricing route to navigation.

STEP 5 — Do not modify: email gate, question counter, 
chat functionality, or any existing components.